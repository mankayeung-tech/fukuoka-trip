# 2026-03-29 - 福岡之旅地圖修復與行程優化

## 完成事項
- **地圖修復**：修正 `fukuoka-trip-map.html` 中 `locations` 陣列在 Day 5 的語法錯誤（缺少 `{`）。
- **行程數據校對**：確認 5 日共 34 個行程點的 JSON 結構完整，標點符號（逗號、括號）正確。
- **交互功能測試**：確保 `dayLayers` 和 `allMarkers` 能正常過濾和顯示。
- **雙評分系統**：在所有地圖標記的描述（`desc`）和 `index.html` 中加入了 Tabelog (T) 與 Google (G) 的雙重評分，方便大哥決策。

- **行程同步與優化**：為 `fukuoka-trip-map.html`、`index.html` 及 Markdown 文件全面補回每日「酒店出發」起點，確保行程邏輯一致。
- **里程計算功能**：在地圖側邊欄（Legend）中實時顯示每日預計總里程（km），並在 Marker Popup 中加入「距上一站」的距離顯示，方便大哥掌控行程節奏。
- **開支紀錄功能**：喺 `index.html`（備忘資訊分頁）同 Markdown 行程表中新增「實際開支紀錄」區塊，目前已記錄機票 ($1,770.00)、旅遊保險 ($316.72) 同酒店 ($7,543.04)，並自動計算目前總開支 ($9,629.76)。
- **最終核對**：完成所有數據與 UI 的最終校對，確保互動地圖、行程表及開支紀錄完美同步。

## 後續建議
- 所有技術實現（Notion 同步、地圖互動、里程計算）已完成，建議大哥可以正式將整個 folder 上傳至 Netlify 進行部署，方便在旅途中隨時查閱。
- 由於 6 月是梅雨季，提醒大哥隨時留意當地的天氣預報，隨時調整戶外與室內（如美術館）的先後順序。
