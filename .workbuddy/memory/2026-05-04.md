## 2026-05-04

### 部署問題排查與解決
- 發現 Cloudflare Pages 自動部署失敗：GitHub `main` branch 已更新到 `b794960`（Vacation® redesign），但 `fukuoka-trip.pages.dev` 仍然顯示舊版本（`✈️ 福岡四日行程計畫`，2025年9月）。
- 診斷：Cloudflare Pages 項目 `fukuoka-trip` 的 subdomain 實際為 `fukuoka-trip-d18.pages.dev`，`fukuoka-trip.pages.dev` 已被其他項目保留。
- 使用 Wrangler CLI (`npx wrangler pages deploy`) 進行 Direct Upload 部署成功。
- 需要提供 `CF_ACCOUNT_ID` 環境變量才能繞過 `/memberships` 權限檢查。
- 新部署網址：https://fukuoka-trip-d18.pages.dev

### PWA Icon 加入
- 將用戶提供的鳥居 icon（珊瑚橙色背景）複製為 `apple-touch-icon.png`（2304x1406）。
- 在 `index.html` 加入 `apple-touch-icon`、`icon`、`manifest.json` link，以及 iOS PWA meta tags（`apple-mobile-web-app-capable`、`apple-mobile-web-app-status-bar-style`、`apple-mobile-web-app-title`）。
- 新增 `manifest.json`（name: 福岡慢活放鬆之旅 2026，short_name: 福岡之旅，theme_color: #f1d27a，background_color: #e5e7eb）。
- Commit `35b4694`：Add PWA icon and manifest，已 push 到 GitHub。

### 最新線上版本
- 網址：https://fukuoka-trip-d18.pages.dev
- 包含：Vacation® redesign + PWA icon/manifest + 正確行程內容（2026年6月7-11日）
